name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.5.0-beta.9'
      SERVERPOD_PASSWORD_database: 'GEhzJb0Q-IRsrA8M6yuJdM8rZacxaPNP'
      SERVERPOD_PASSWORD_redis: 'lonxHfYrgx18jKb3BDkc82SgJC3qRqYn'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: liqd_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: liqd_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: liqd_server
        run: dart install serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: liqd_server
        run: serverpod generate

      - name: Run tests
        working-directory: liqd_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: liqd_server
        run: docker compose down -v
