name: liqd_a2ui
description: Shared A2UI extraction, validation, and prompt assembly for Liqd.
publish_to: none

environment:
  sdk: '^3.10.3'

resolution: workspace

dependencies:
  json_schema_builder: ^0.1.3

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  test: ^1.25.5
